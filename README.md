# FocusTime - React Native

Simple app that implements basics of React Native

## Instalation

1. run `yarn` to install all the packages
2. run `yarn start` or `yarn (android/ios)` to run on emulators
